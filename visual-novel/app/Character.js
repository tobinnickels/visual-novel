export default class Character{
    constructor(name,unlocked,currentPage,maxPage){
      this.name = name;
      this.unlocked = unlocked;
      this.currentPage = currentPage;
      this.maxPage = maxPage;
  }
}
